package spaceinvaders.group_22;
/**
 * Class to handle the incoming events from the UI.
 * @author dorian
 *
 */
public class GameEventControler {

	 /**
     * Create eventhandlers.
     * @param game object to control
     */
    public GameEventControler(final Game game) {
    	/*game.getCanvas().addEventHandler(KeyEvent.KEY_PRESSED, 
                new EventHandler<KeyEvent>() {
					public void handle(KeyEvent e) {
						Main.getGame().getGameControler.keyEvent(e);
					}	 
				});*/
    }
}
